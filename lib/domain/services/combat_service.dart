/// Abstract contract for combat resolution logic.
///
/// Turn resolution, damage calculations, and status effect application belong
/// here so that [CombatController] delegates to this service rather than
/// embedding formulas directly.
abstract interface class CombatService {
  // Placeholder — methods will be added when CombatModel is defined.
}
