/// Abstract contract for hero persistence.
///
/// Concrete implementations live in `lib/data/`. The domain and controllers
/// depend only on this interface, keeping them testable without I/O.
abstract interface class HeroRepository {
  // Placeholder — methods will be added when HeroModel is defined (CNT tasks).
}
